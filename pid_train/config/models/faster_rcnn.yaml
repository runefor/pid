model:
  _target_: pid_train.models.architecture.ObjectDetector
  model:
    _target_: pid_train.models.factory.create_faster_rcnn
    num_classes: 146
    pretrained: true
    gradient_checkpointing: true
  val_requires_targets: true # 검증 시 targets 필요 없음 플래그